# Footage never enters the repo. It is a retailer's CCTV.
*.mp4
*.mkv
*.avi
*.mov
*.MOV
*.MP4
*.h264
*.h265
*.dav

# Pipeline outputs (derived from footage)
runs/

# Python
__pycache__/
*.pyc
.venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Model weights (downloaded once, kept local)
models/
*.pt

# Marked frames and training runs (camera pictures of people)
labels/
training/

# App settings and packaging output
settings.json
build/
dist/
bench/
retailnext/

# Camera drawings: the user's own, never touched by an update
sites/

# Written into the app by GitHub's build (packaging/build_info.py): build number, built-in keys
src/crossing_count/build.json
src/crossing_count/builtin.dat

# Gold set: counts of a retailer's footage
gold/
